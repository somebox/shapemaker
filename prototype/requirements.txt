# Dependencies for the Python prototype and the acceptance gate.
#
# Pinned to compatible-minor ranges because the prototype is the reference the
# JS port is validated against: a trimesh or scipy change to mesh processing
# would move those numbers, and a reference that drifts is worse than none.
# These are the versions the M1 parity figures (7200 tris, 16.150 cm3) were
# verified with.
numpy~=2.5.1
scipy~=1.18.0
trimesh~=4.12.2
shapely~=2.1.2
rtree~=1.4.1
pillow~=12.3.0
